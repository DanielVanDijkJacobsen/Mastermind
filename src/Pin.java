//This class can set and retrieve a given value, it represents the different pins used in Mastermind

public class Pin {
    int pinValue;

    Pin (int value) {
        pinValue = value;
    }

    //Cheat code made to check the value of a given pin in the game during debugging.
    /*void showValue() {
        System.out.println(pinValue);
    }*/
}
